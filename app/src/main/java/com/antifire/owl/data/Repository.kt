package com.antifire.owl.data

/**
 * Base repository interface that all data repositories must implement.
 * 
 * Phase 2 Foundation - Data layer base interface.
 * 
 * This establishes the repository pattern used throughout the data layer.
 * Concrete repositories will be added in later phases as data sources
 * become available.
 */
interface Repository
